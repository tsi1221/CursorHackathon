# FixMyHood Client

React + Vite + Tailwind UI for FixMyHood Addis Ababa.

## Setup

```bash
npm install
npm run dev
```

Set API base by copying `.env.example` to `.env` and updating `VITE_API_URL` if needed.